#include <stdio.h>

/**
 * main - The program
 * @argc: count of the args
 * @argv: the actual arguments
 * Return: Exit code
 */
int main(int argc, int *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

