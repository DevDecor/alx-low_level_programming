#include <stdio.h>
#include "main.h"

/**
 * _islower - Prints all the lowercase alphabets ten times
 * @c: character to be checked
 * Return: void
 */
void _islower(int c)
{
	if (c < 97)
		return (1);
	else
		return (0);
}
